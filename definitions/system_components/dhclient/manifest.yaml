manifest_scheme_revision: 1
application:
    name: dhclient
    version: 1.0
definition:
    libregistry_minimum_version: 0.1.0

structure:
    dhclient_conf: dhclient.conf

detect_installed:
    - /etc/dhcp/dhclient.conf
    - /sbin/dhclient
